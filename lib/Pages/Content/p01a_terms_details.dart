import 'package:flutter/material.dart';

class TermsDetailsPage extends StatelessWidget {
  final int index;
  const TermsDetailsPage(this.index, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
